spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
logging:
  level:
    root: INFO
    com.hanrry.studytracker: INFO
    org.hibernate.SQL: ERROR